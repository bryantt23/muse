- @posts.each do |post|
  = image_tag post.image.url(:small)
  = image_tag post.image.url(:medium)
  %h2= link_to post.title, post
  %p
    = post.comments.count
    Comments
  %p
    = post.get_likes.size
    Likes

= link_to "New Inspiration", new_post_path